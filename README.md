# Sample_Repo
This repository is created as an environment to test the functionality of GitHub.

This line is now added in a local clone of the repo to test whether we can push it to the GitHub instance. 